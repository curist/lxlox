let types = import "src/types.lx"

fn printAllKeys(m) {
  print("========================")
  let kk = keys(m)
  for let i = 0; i < len(kk); i = i + 1 {
    print(kk[i])
  }
}

fn printArray(arr) {
  print("========================")
  for let i = 0; i < len(arr); i = i + 1 {
    print(arr[i])
  }
}
printAllKeys(types)

printArray(globals())
