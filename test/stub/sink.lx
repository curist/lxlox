// let types = import "src/types.lx"
let mod1 = import "test/stub/import.lx"
let mod2 = import "test/stub/module.lx"

fn printAllKeys(m) {
  print("========================")
  let kk = keys(m)
  for let i = 0; i < len(kk); i = i + 1 {
    print(kk[i])
  }
}

fn printArray(arr) {
  print("========================")
  for let i = 0; i < len(arr); i = i + 1 {
    print(arr[i])
  }
}
// printAllKeys(mod2)

printArray(globals())
