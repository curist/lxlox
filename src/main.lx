let lib = import "src/lib.lx"

fn pp(arr) {
  if type(arr) == "array" {
    print(lib.join(arr, ""))
  } else {
    print(arr)
  }
}

pp("hello world!")
print(keys(lib))

pp([1234, ":\t", lib.tohex(1234)])
pp([255, ":\t", lib.tohex(255)])
pp([16, ":\t", lib.tohex(16)])
